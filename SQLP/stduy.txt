*p.60

이 질문은 데이터베이스에서 **Multiblock I/O**가 진행되는 도중에 왜 **Single Block I/O**가 발생하는지에 대한 문제입니다. 이를 이해하기 위해서는 각 용어와 과정을 차근차근 살펴보는 것이 중요합니다.

### 1. **익스텐트 맵(Extent Map)**

- **익스텐트(Extent)**는 데이터베이스에서 테이블이나 인덱스가 저장되는 연속적인 데이터 블록 그룹입니다. 데이터베이스가 데이터를 저장할 때, 이 데이터가 여러 개의 블록에 나누어 저장되는데, 이러한 블록들의 그룹을 **익스텐트**라고 합니다.
- **익스텐트 맵**은 데이터베이스에서 익스텐트가 어느 블록에 저장되었는지에 대한 정보가 담긴 인덱스와 같은 역할을 합니다. 이를 통해 특정 데이터를 찾기 위해 어떤 블록들을 읽어야 하는지 알아낼 수 있습니다.

### 2. **Multiblock I/O**

- **Multiblock I/O**는 한 번에 여러 개의 블록을 읽는 방식입니다. 대량의 데이터를 처리할 때, 여러 블록을 동시에 읽는 것이 효율적이기 때문에 이 방식을 사용합니다. 예를 들어, 한번에 4개의 블록을 읽어들인다고 가정하면, 이는 **Multiblock I/O 단위가 4**라는 의미입니다.
- **Batch I/O**라고도 불리는 Multiblock I/O는 대량의 데이터를 빠르게 처리하기 위해 설계되었습니다.

### 3. **Single Block I/O**

- **Single Block I/O**는 한 번에 한 개의 블록만 읽는 방식입니다. 특정 블록만을 읽어야 할 때 사용되며, Multiblock I/O에 비해 느리지만 메모리에 없는 특정 데이터를 불러올 때 사용됩니다.

### 4. **캐시버퍼 체인(Cache Buffer Chain)**

- **캐시버퍼 체인**은 메모리에 저장된 데이터 블록들이 연결된 구조입니다. 데이터베이스는 디스크에서 데이터를 읽기 전에 메모리(캐시)에 이미 해당 데이터가 있는지 먼저 확인합니다. 이때 캐시버퍼 체인에서 필요한 데이터 블록을 찾습니다.

---
**Multiblock I/O 단위가 4라고 가정하고, 익스텐트 맵에서 1번에서 10번까지의 블록을 읽는 상황을 가정해 봅시다.**

1. **첫 번째 블록(1번)**:
   - 캐시버퍼 체인에서 **1번 블록**을 찾습니다.
   - **1번 블록**이 이미 메모리에 있기 때문에 디스크 접근 없이 바로 **메모리에서 읽습니다**. 즉, **캐시에서 읽는 작업**이 발생합니다.

2. **2번, 3번, 4번, 5번 블록**:
   - 캐시버퍼 체인에서 **2번, 3번, 4번, 5번 블록**을 찾습니다.
   - 이 블록들은 캐시에 없으므로 **디스크에서 읽어야 합니다**.
   - 이때, **Multiblock I/O** 방식을 사용하여 **2번부터 5번까지**의 블록을 한 번에 읽어들입니다.
   - 이 과정을 통해 **디스크에서 읽은 데이터**는 메모리에 저장되고, 바로 사용됩니다.

3. **6번 블록**:
   - 캐시버퍼 체인에서 **6번 블록**을 찾습니다.
   - **6번 블록**이 메모리에 있으므로 바로 **캐시에서 읽습니다**.

4. **7번 블록**:
   - 캐시버퍼 체인에서 **7번 블록**을 찾습니다.
   - **7번 블록**이 메모리에 없으므로 **디스크에서 읽어야 합니다**.
   - 이때는 **Multiblock I/O** 대신 **Single Block I/O** 방식이 사용됩니다. 왜냐하면 7번 블록을 읽을 때 8번 블록은 이미 메모리에 있기 때문입니다. 따라서 7번 블록만 **디스크에서 읽어** 옵니다.

5. **9번, 10번 블록**:
   - 캐시버퍼 체인에서 **9번 블록**을 찾습니다.
   - **9번 블록**이 메모리에 캐시되어 있으므로 바로 **캐시에서 읽습니다**.
   - **10번 블록**은 캐시에 없으므로 **디스크에서 읽어야 합니다**.
   - 이때, **10번 블록은 익스텐트의 마지막 블록이기 때문에** 다시 **Single Block I/O** 방식으로 디스크에서 읽습니다.

---

### 왜 **Single Block I/O**가 나타나는가?

1. **캐시 상태**: 
   - 일부 블록은 메모리 캐시에 이미 존재하기 때문에, **Multiblock I/O**가 아닌 **Single Block I/O**가 발생합니다. 캐시된 블록을 제외한 나머지 블록만 디스크에서 읽어야 하기 때문에 Multiblock I/O가 끊어질 수 있습니다.

2. **익스텐트의 끝**: 
   - **익스텐트의 마지막 블록**에서는 **Single Block I/O**가 사용됩니다. 이는 데이터베이스가 마지막 블록을 처리할 때 Multiblock I/O 단위로 데이터를 읽는 것이 효율적이지 않기 때문입니다.

### 요약

- **Multiblock I/O**는 한 번에 여러 개의 블록을 읽어 효율을 높이는 방식이며, **Single Block I/O**는 특정 블록을 읽기 위해 사용됩니다.
- **Single Block I/O**는 주로 메모리에 없는 특정 블록을 읽거나, 익스텐트의 마지막 블록을 처리할 때 사용됩니다.

p.62
카디널리티(Cardinality)는 데이터베이스에서 **특정 컬럼의 고유 값의 수**를 나타내는 용어입니다. 즉, 해당 컬럼에 얼마나 다양한 값이 들어있는지를 의미합니다.

### 예시:
- 예를 들어, `고객` 테이블에서 `성별`이라는 컬럼이 있다고 합시다. 이 컬럼은 `남성`과 `여성`이라는 두 가지 값만을 가질 수 있습니다. 이 경우, `성별` 컬럼의 카디널리티는 2입니다.
- 반면에 `주민등록번호`라는 컬럼은 각 고객마다 고유한 값을 가집니다. 따라서 `주민등록번호` 컬럼의 카디널리티는 테이블의 전체 행 수와 같습니다. 즉, 카디널리티가 매우 높습니다.

### 카디널리티와 SQL 튜닝:
- **카디널리티가 낮은 경우** (즉, 컬럼의 고유 값이 적을 때), 해당 컬럼에 인덱스를 사용해도 성능 개선에 큰 도움이 되지 않을 수 있습니다. 왜냐하면 인덱스를 통해 데이터를 찾는 작업이 많아지고, 결국 테이블 전체를 스캔하는 것과 차이가 없어지기 때문입니다.
- **카디널리티가 높은 경우** (즉, 고유 값이 많은 컬럼), 인덱스를 사용하면 성능이 크게 향상될 수 있습니다. 고유 값이 많기 때문에 인덱스를 통해 정확하게 데이터를 찾을 수 있기 때문입니다.

튜닝에서 중요한 것은 어떤 상황에서 인덱스를 사용하고, 어떤 상황에서 Table Full Scan이 더 나은지**를 판단하는 것입니다. 카디널리티가 중요한 이유는 이를 통해 인덱스 사용의 효율성을 판단할 수 있기 때문입니다.

p107.

- 계좌번호 =: acnt_no -> 계좌번호에 변수를 넣게 해주는것 :=acnt_no는 쿼리 실행시 원하는 계좌번호 할당하여 실행

p108.
형변환의 오해
DECODE(a,b,c,d)를 한번 알아보면 a=b 가 true라면 c를 retrun 하고 false면 d를 return 하는 함수이다.
DECODE에서는 결과를 출력 할때 c의 데이터타입에 따라 반환 데이터타입이 결정되어진다.

예시 
-어떤반에서 선생님과 학생 모두 시험을 응시한 상황
 선생님을 제외한 학생들의 성적중 가장 높은 점수를 얻은학생을 찾기

 - 시험점수 Table -
 철수 100
 창섭 99
 민수 90
 선생님 98

 query
 SELECT MAX(DECODE(이름,'선생님',NULL,점수))AS 최고점수_학생

 이쿼리를 보면 최고점수 학생 철수가 나올것 이라고 생각했지만 최고학생이 안나온다.
 
 왜 그런걸까?
 - DECODE(a,b,c,d)에서 return 데이터 타입은 true 리턴인 c의 데이터타입을 따르는데
   여기에서 c는 NULL이다. 여기 NULL은 DECODE에서 VARCHAR2로 취급한다. 
   결국 리턴타입VARCHAR2로 MAX연산을 하면 결국 100보다 더큰 문자열인 99가 출력되어진다.

solution
 - 리턴타입의 데이터 타입을 명시적으로 표기한다.
 - SELECT MAX(DECODE(이름,'선생님',TO_NUMBER(NULL),점수))AS 최고점수_학생
 - SELECT MAX(DECODE(이름,'선생님',CAST(NULL AS NUMBER),점수))AS 최고점수_학생 (CAST를 이용한 명시)

 p111.
 SQL 옵티마이저 스캔 종류
 - Index Range Scan

 - Index Full Scan

 - Index Unique Scan

 - Index Skip Scan

 - Index Fast Full Scan

 - Index Range Scan Descending
 

p151.
# convered 쿼리
- 테이블 액세스를 하지 않고 인덱스만 읽어서 처리하는 쿼리

# include 인덱스
- 인덱스 키 외에 미리 지정한 컬럼을 리프레벨과 함께 저장
- ex 1 : create index emp_x on  emp (deptno) include (sal) (include on)
- ex 2 : create index emp_x ont emp(deptno,sal)(include off)

만약에 
select * from emp where deptno = 20 and sal >=2000
select * from emp where deptno = 20 and sal between 2000 and 3000
이런 sql문을 생각해보면 
결국 ex2는 인덱스 소트 연산이 생략 가능하지만 ex1은 인덱스를 생략하지 못한다.

so. include인덱스는 온전히 랜덤 액세스를 줄이는 용도로 개발되어졌다.

p153.
# 인덱스 구조 테이블
 테이블 액세스가 고비용 구조기 때문에 랜덤 액세스가 발생하지 않도록 인덱스 구조를 생성하면?
 
 IOT(oracle),클러스터형인덱스(sql-server)

IOT 클러스터형인덱스
- ROWID를 갖는 일반 인덱스와 달리 그자리에서 테이블 데이터를 갖는다.
- 데이터 블록에 있어야 할 데이터를 그냥 인덱스 리프 블록에 모두 저장하는구조

ex
  일반 테이블: 김금영 rowin --> 김금영 남 7500 (인덱스 블록 --> 데이터 블록 : 독립적)
  인덱스 구조 테이블: 김금영 남 7500 (인덱스 리프 블록에 모두 저장)

테이블을 IOT구조로 만드는 구문예시
  create table index_org_t ( a number,  b varcher(10), constraint index_org_t_pk primary ket(a))
  organization index; (이렇게 인덱스구조 테이블 옵션 명시)

왜 인덱스 구조 테이블을 사용할까?
- 일반적인 테이블인 힙 구조 테이블은 데이터를 입력할때 랜덤방식 사용(freelist)
- 인덱스 구조 테이블은 랜덤이 아니라 정렬상태를 유지하며 데이터 입력 
- 즉 인덱스 구조테이블은 인위적으로 클러스터 팩터링(CF)를 좋게 만드는 방법 중 하나

- 클러스터팩터링이 좋으면 BETWEEN이나 부등호 연산시 넓은범위를 읽을때 좋은 성능을 발휘하는데
- 인덱스 구조 테이블을 통해 넓은 범위를 읽을떄 좋은 성능을 발휘한다.

- 데이터 입력이나 조회 패턴이 서로 다른 테이블에서도 유용.
ex: 
select substr(일자, 1, 6)월도, sum(판매금액) 총 판매금액 , avg(판매금액) 평균판매금액
from 영업실적
where 사번 = 's1234' and 일자 between '20180101' and '20181231'
group by substr(일자,1,6)
해당 쿼리에 인덱스를 사용한다면 사원마다 랜덤액세스 형식으로 365개의 테이블 블록을 읽어옴

so. 랜덤액세스형식 이므로 클러스터 팩터링이 안좋다면 많은 블록 I/O가 발생한다.
즉 한블록에 100개의 레코드가 저장된다면
랜덤액세스는 365개의 블록을 전부 읽어야 하지만 인덱스 구조 테이블 사용시
단 4개의 블록만 읽으면 된다.

#클러스터 테이블
-인덱스 클러스터 테이블
-해시 클러스터 테이블

인덱스 클러스터 테이블
- 클러스터 키의 값이 같은 레코드들을 한 블록에 모아서 저장하는 구조
- 다중 클러스터 테이블 : 하나의 데이터 블록에 여러테이블 레코드를 저장
ex.
cluster key : deptno
블록 1
deptno: 10
7839 KING PRESIDENT 5000
7782 CLARK MANAGER 7839 2450
7934 MILLER CLERK 7782 1300
블록 2
deptno: 20
7788 SCOTT ANALYST 3000
7902 FORD ANALYST 3000
7566 JONES MANAGER 2975
7876 ADAMS CLERK 1100

인덱스 클러스터 구문 예시
1. 클러스터 생성
create cluster c_dept# (deptno number(2)) index;
2. 인덱스 생성
create index c_dept#_idx on cluster c_dept# 
-> 클러스터 인덱스를 반드시 정의해야함. 인덱스는 검색 뿐만아니라 저장할때도 사용하기 때문
3. 클러스터 테이블 생성
create table dept(
   deptno number(2) not null,
   dname varcher(14) not null,
   loc varcher(13))
   cluster c_dept#(deptno);


해시 클러스터 테이블
- 해시 클러스터는 인덱스를 사용하지 않고 해시 알고리즘을 사용해 클러스터를 찾아가는점만 다르다.

해시 클러스터 테이블 구문
1.클러스터 생성
create cluster c_dept# (dept number(2)) hashkeys 4;
2. 클러스터 테이블 생성
create table dept(
   deptno number(2) not null,
   dname varcher2(14) not null,
   loc varchar2(13))
   cluster c_dept#(deptno);

p159.

#부분범위 처리 활용
부분범위 처리
- DBMS에서 데이터를 모두 읽어 한번에 전송하는것이 아니라 일정량 데이터를 전송하고 멈추는 활동

1108 P165. 3.2.1 까지 읽음

p141.
인덱스 스캔 효율화

일반적인 튜닝기법은 인덱스컬럼을 추가하는것

3.3.1 인덱스 탐색

수직적 탐색-> 스캔시작점을 찾는 과정
만약 a3 b3이 있다면 b3 리프블록으로 내려가는것이 아니라 a3리프로 내려가 b의 시작지점을 찾는다.

인덱스 스캔의 효율성 -> 선행 컬럼을 조건에 포함했는지 여부와 조건이 정확히 일치하는지에 따라 달라진다.
효율적인 인덱스 스캔을 위해서는 선행 컬럼을 조건에 포함시키거나 = 조건을 사용하는 것이 좋다.

3.3.3 액세스 조건, 필터 조건

액세스 조건
- 인덱스 액세스 조건 : 인덱스 수직적탐색을 통해 시작점을 결정하고 어디에서 멈출것인지 영향을 미치는 조건절

필터 조건
- 인덱스 필터 조건 : 테이블로 액세스할지 결정하는 조건절

1.인덱스스캔을 했는데도 찾고자하는 데이터가 흩어져있다 -> 인덱스 필터스캔으로 데이터 찾는다.


11-11 3.3.6 (p200)

p212
between 과 like의 차이 
- 왠만하면 like말고 between사용하자
- between이 왠만한 상황에서 비효율적인 스캔범위가 줄어든다

예를 들어서 
1.
where 판매월 between '190101' and '191231'
and 판매구분 ='a'
2.
where 판매월 like '2019%'
and 판매구분 = 'a'

1과 2는 2019년의 판매구분 'a'인 레코드들을 확인하려는 쿼리문인데
1에서는 20190101 a 에서 20191231 a 까지 스캔을 하지만
2에서는 20190101 a에서 20191231 b 까지 스캔을 한다
즉 2에서는 스캔범위 밖인 20191231 b 까지 비효율적이 스캔을 한다. 

성능 최적화를 위한 NVL/DECODE 함수 활용
NVL과 DECODE함수를 이용하면 옵선조건을 인덱스 액세스 조건으로 활용하여 쿼리 성능 최적화가 가능합니다.

예를들어
SELECT * FROM 거래
WHERE 고객ID = NVL(:cust_id, 고객ID)
AND 거래일자 BETWEEN :dt1 AND :dt2;

일단 먼저 NVL함수가 어떤 역활을 하는지 알아보면
column = NVL(A,B) -> 해당 column이 NULL값이라면 A를 반환 NULL이 아니라면 B를 반환합니다.

그럼 위의 쿼리문을 자세히 보면 cust_id의 값이 NULL이라면 고객ID = 고객ID가 되어서 전체 고객이 선택되어지고,
NULL이 아니라면 특정고객을 조회합니다.

이와 비슷하게 DECODE에서의 예시를 살펴봅시다.

SELECT * FROM 거래
WHERE 고객ID = DECODE(:cust_id,NULL,고객ID,:cust_id)
AND 거래일자 BETWEEN :dt1 AND :dt2;

DECODE함수에 대해서 알아보면 
DECODE(A,B,C,D) -> 만약 A가 B라면 C를 반환하고 아니면 D를 반환한다.

즉 예시 쿼리에서 DECODE는 :cust_id 가 NULL이면 고객ID = 고객ID를 반환하고 , NULL이 아니라면 특정고객을 조회합니다.

위 두가지의 예시쿼리는 같은 기능을 수행한다는것을 알 수 있습니다.
이렇게 NVL/DECODE를 이용하여 조건에 따라 조회범위를 다르게 함으로써 옵션조건을 효율적으로 활용하고,
불필요한 테이블 스캔을 줄여 쿼리 성능을 최적화 할 수 있습니다.

NVL/DECODE를 이용하여 성능 최적화시 주의사항
만약 옵션 조건 컬럼이 NULL을 허용하는 컬럼이라면 SQL의 특성(NULL은 비교연산을 수행안함)상 NULL처리를 못할 수 있습니다.
즉 NULL조건은 참이 되어지지 않기 때문에 조건절에서 NULL처리를 주의해야합니다.












