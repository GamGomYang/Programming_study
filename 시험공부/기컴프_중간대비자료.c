/*주석,변수에 대한 이해
-> 주석의 개념, 사용법 / 변수의 선언, 초기화

*형식 지정 및 필드폭에 대한 이해
-> 예) 실수표현에 있어 printf(“%5.1f\n“, 456.456); 의 결과는?

*C언어에서 10진수 2진수 8진수 16진수를 표현 하는 방법(22일 수업시간에 자세히)
10진수 숫자 11을 표현한다면
2진수로는 1011 또는 0b1011 로 표현 (접두어로 ‘0b’(binary의 b)를 쓰거나 쓰지 않음)
8진수로는 013 (접두어로 숫자 ‘0’을 붙임)
16진수로는 0xB (접두어로 ‘0x’를 붙임) (x는 he‘x’adecimal의 x를 씀)

*정수든 실수든 여러 가지 형태를 쓰는 이유는 더 정확하고 세밀한 표현을 가능하게 한다.
(C언어에서 float 보다 double이 더 범주가 넓다)

*증감연산자에 대한 이해
->전치인지 후치인지 언제 값의 변화가 이뤄지는지

*형변환을 했을 때 연산 결과
-> 예를들어 정수형 변수를 실수형으로 변환뒤 나누기

*조건 연산자(삼항연산자)에 대한 이해
-> 조건식? 참 : 거짓

*관계연산자와 논리연산자에 대한 이해 및 실행 순서
ex) n<=10 && k>5 && p==1
1)관계 연산자 먼저 실행(참인지 거짓인지)
2)n<=10 && k>5의 논리연산 실행
3)2)의 결과와 && p == 1 의 논리연산 실행



이제 각 개념에 대한 설명과 예시 코드를 준비하고, 문제와 해설을 포함한 자료를 차근차근 정리해드리겠습니다. 개념 설명과 예제 코드를 제공한 뒤, 이 내용으로 문제를 생성하여 해설과 함께 구성하겠습니다. 

---

### 1. 주석과 변수의 이해

**주석**  
주석은 코드에 설명을 추가하는 부분으로, 컴파일러에 의해 무시됩니다.  
- `//`: 한 줄 주석을 추가할 때 사용합니다.
- `/* ... */`: 여러 줄 주석을 추가할 때 사용합니다.

**예시**:
```c
// 이것은 한 줄 주석입니다
/*
   여러 줄 주석입니다
   두 번째 줄
*/
```
/*
**변수**  
변수는 데이터를 저장하는 공간입니다. 선언과 초기화가 필요합니다.
- 선언: 변수를 생성하고 자료형을 지정합니다. `int num;`
- 초기화: 변수를 생성과 동시에 값으로 초기화합니다. `int num = 10;`

**예시**:
```c
int num;        // 변수 선언
num = 5;        // 값 대입
int age = 25;   // 선언과 초기화
```

---

### 2. 형식 지정자와 필드폭

C 언어에서 `printf` 함수는 다양한 형식 지정자를 이용해 출력 형식을 지정할 수 있습니다.
- `%d`: 정수
- `%f`: 실수
- `%c`: 문자
- `%s`: 문자열

**필드 폭**은 출력할 때 자리수를 지정하여 자리수를 확보할 수 있습니다.
- 예: `printf("%5.1f\n", 456.456);`
  - `5.1`에서 `5`는 전체 자리 수, `.1`은 소수점 아래 자리 수입니다.
  - 출력 결과: `456.5`

---

### 3. 진법 표현

각 진법의 표현 방법:
- **10진수**: 일반적인 정수(예: `11`)
- **2진수**: 접두어 `0b`를 붙여 2진수로 표시(예: `0b1011`)
- **8진수**: 접두어 `0`을 붙여 8진수로 표시(예: `013`)
- **16진수**: 접두어 `0x`를 붙여 16진수로 표시(예: `0xB`)

**예시**:
```c
int decimal = 11;
int binary = 0b1011;
int octal = 013;
int hex = 0xB;

printf("10진수: %d\n2진수: %d\n8진수: %o\n16진수: %x\n", decimal, binary, octal, hex);
```

---

### 4. 정수와 실수의 표현

정수와 실수를 다양한 형태로 사용하는 이유는 표현의 정확도를 높이기 위함입니다.
- `float`: 단정도 실수, 소수점 이하 약 6자리 정밀도 제공.
- `double`: 배정도 실수, 소수점 이하 약 15자리 정밀도 제공.

---

### 5. 증감 연산자

- `++`: 증가 연산자
- `--`: 감소 연산자
- 전치(`++a`): 값을 먼저 증가시키고 사용
- 후치(`a++`): 값을 사용한 후 증가

**예시**:
```c
int a = 5;
printf("전치 증가 연산: %d\n", ++a);  // 6
a = 5;
printf("후치 증가 연산: %d\n", a++); // 5, 이후 a는 6
```

---

### 6. 형 변환

형 변환은 변수나 값을 특정 자료형으로 변환하여 계산하는 것을 말합니다. 예를 들어 정수와 실수를 나누려면 실수형 변환이 필요할 수 있습니다.

**예시**:
```c
int a = 10;
int b = 3;
printf("%f\n", (float)a / b); // 실수형으로 변환하여 나눔
```

---

### 7. 조건 연산자 (삼항 연산자)

조건 연산자는 `조건식 ? 참일 때 값 : 거짓일 때 값`의 형태로 사용합니다.

**예시**:
```c
int a = 10, b = 5;
int max = (a > b) ? a : b;  // a가 크면 a, 그렇지 않으면 b
```

---

### 8. 관계 연산자와 논리 연산자

- 관계 연산자: `==`, `!=`, `<`, `>`, `<=`, `>=`
- 논리 연산자: `&&` (논리 AND), `||` (논리 OR), `!` (논리 NOT)

**예시**:
```c
int n = 8, k = 6, p = 1;
if (n <= 10 && k > 5 && p == 1) {
    printf("조건이 모두 참입니다.");
}
```

---

### 문제 예시 및 해설 (총 5문제)

**문제 1:** 주석의 기능은 무엇인가요?
   - (1) 코드에 설명을 추가하여 가독성을 높입니다. **[정답]**
   - (2) 변수의 값을 초기화합니다.
   - (3) 함수를 호출합니다.
   - (4) 메모리를 할당합니다.

**해설:** 주석은 코드에 설명을 추가하는 데 사용되며, 가독성 향상에 도움을 줍니다.

---

**문제 2:** `printf("%5.1f", 456.456);`의 출력 결과는?
   - (1) 456.456
   - (2)  456.5 **[정답]**
   - (3) 456.4
   - (4)  456

**해설:** `5.1`에서 전체 자리 수는 5, 소수점 이하 자리 수는 1로 출력됩니다.

---

**문제 3:** 10진수 `11`을 8진수로 변환한 결과는?
   - (1) 1011
   - (2) 11
   - (3) 013 **[정답]**
   - (4) 0xB

**해설:** 8진수는 접두어로 `0`을 붙이며, 11은 8진수로 `013`입니다.

---

**문제 4:** `++a`와 `a++`의 차이는 무엇인가요?
   - (1) 전치 연산은 값을 먼저 증가시키고, 후치 연산은 나중에 증가시킵니다. **[정답]**
   - (2) 전치 연산은 나중에 증가시키고, 후치 연산은 먼저 증가시킵니다.
   - (3) 전치 연산은 감소시키고, 후치 연산은 증가시킵니다.
   - (4) 전치 연산은 변수 값을 변경하지 않습니다.

**해설:** 전치 연산은 값을 먼저 증가시키고, 후치 연산은 나중에 증가시킵니다.

---

**문제 5:** 다음 중 조건 연산자를 사용한 올바른 표현은 무엇인가요?
   - (1) `a > b ? a : b;` **[정답]**
   - (2) `a = b ? a : b;`
   - (3) `a ? b : c`
   - (4) `a < b ? : c`

**해설:** 조건 연산자는 `조건식 ? 참일 때 값 : 거짓일 때 값`의 형태로 사용합니다.

---

좋습니다. 이어서 나머지 개념에 대해 25개의 문제를 추가로 작성하고 해설을 제공해드리겠습니다. 

---

**문제 6:** 변수 `int x = 5;`에서 `++x`를 출력할 경우 결과는?
   - (1) 4
   - (2) 5
   - (3) 6 **[정답]**
   - (4) 7

**해설:** 전치 증가 연산자(`++x`)는 `x` 값을 먼저 증가시킨 뒤 출력합니다. 초기값 5에서 1 증가하여 6이 됩니다.

---

**문제 7:** 변수 선언과 초기화의 차이에 대해 설명할 때 올바른 것은?
   - (1) 선언은 변수를 생성하고 초기화는 변수에 값을 대입하는 과정입니다. **[정답]**
   - (2) 초기화는 변수를 생성하는 과정입니다.
   - (3) 선언은 변수에 값을 대입하는 과정입니다.
   - (4) 선언과 초기화는 같은 의미입니다.

**해설:** 선언은 변수의 메모리 공간을 할당하고, 초기화는 변수에 값을 저장하는 단계입니다.

---

**문제 8:** 실수형 변수 `float`과 `double`의 차이점은?
   - (1) `float`은 정수, `double`은 실수를 저장합니다.
   - (2) `float`은 4바이트, `double`은 8바이트를 사용합니다. **[정답]**
   - (3) `float`은 8바이트, `double`은 4바이트를 사용합니다.
   - (4) `float`은 정밀도가 낮고 `double`은 정밀도가 같은 값을 저장합니다.

**해설:** `float`은 단정도 실수(4바이트), `double`은 배정도 실수(8바이트)를 사용하며 정밀도가 다릅니다.

---

**문제 9:** `0xB`는 10진수로 어떤 값을 나타내나요?
   - (1) 10
   - (2) 11 **[정답]**
   - (3) 12
   - (4) 13

**해설:** 16진수 `B`는 10진수로 11을 나타냅니다.

---

**문제 10:** 조건 연산자 `a > b ? a : b`는 어떤 값을 반환할까요? (`a = 10, b = 20`일 때)
   - (1) 10
   - (2) 20 **[정답]**
   - (3) 30
   - (4) 참

**해설:** `a > b` 조건은 거짓이므로 `b`의 값인 20을 반환합니다.

---

**문제 11:** 다음 중 주석으로 올바르게 작성된 것은?
   - (1) `/ 주석 내용 /`
   - (2) `/* 주석 내용 */` **[정답]**
   - (3) `// 주석 내용 //`
   - (4) `\\ 주석 내용`

**해설:** `/* ... */`는 여러 줄 주석을 추가할 때 사용하며, `//`는 한 줄 주석을 추가할 때 사용합니다.

---/*

**문제 12:** `printf("%05d", 123);`의 출력 결과는?
   - (1) 123
   - (2) 00123 **[정답]**
   - (3) 12300
   - (4) 01230

**해설:** `%05d`는 전체 5자리 중 앞부분을 `0`으로 채워서 123을 출력합니다.

---

**문제 13:** 관계 연산 `n < 10`이 참일 때 논리 연산자 `&&`를 사용한 조건은?
   - (1) `n < 10 && p == 1` **[정답]**
   - (2) `n < 10 && p != 1`
   - (3) `n == 10 && p == 1`
   - (4) `n > 10 && p == 1`

**해설:** `&&`는 두 조건이 모두 참이어야 전체 조건이 참이 됩니다.

---

**문제 14:** `int x = 10; int y = x++;`에서 `y`의 값은?
   - (1) 10 **[정답]**
   - (2) 11
   - (3) 9
   - (4) 0

**해설:** 후치 연산(`x++`)은 `y`에 `x` 값을 할당한 후 증가하므로 `y = 10`입니다.

---

**문제 15:** 8진수 `012`의 10진수 값은?
   - (1) 12
   - (2) 10
   - (3) 8
   - (4) 10 **[정답]**

**해설:** 8진수 `012`는 10진수로 10을 나타냅니다.

---

**문제 16:** 변수 `float pi = 3.14159;`를 `%.2f`로 출력하면?
   - (1) 3.14 **[정답]**
   - (2) 3.14159
   - (3) 3.141
   - (4) 3.2

**해설:** `%.2f`는 소수점 아래 두 자리까지만 표시하므로 3.14를 출력합니다.

---

**문제 17:** `int a = (int) 5.99;`에서 `a`의 값은?
   - (1) 6
   - (2) 5 **[정답]**
   - (3) 0
   - (4) 5.99

**해설:** `(int) 5.99`는 실수형을 정수형으로 변환하며 소수점을 버려 5가 됩니다.

---

**문제 18:** 삼항 연산자 `a < b ? 1 : 0;`에서 `a=3, b=5`일 때 결과는?
   - (1) 1 **[정답]**
   - (2) 0
   - (3) 3
   - (4) 5

**해설:** `a < b` 조건이 참이므로 참일 때 값인 `1`이 반환됩니다.

---

**문제 19:** `n >= 5 && p == 1`의 실행 순서가 올바른 것은?
   - (1) `&&` 논리 연산 후 관계 연산
   - (2) `n >= 5` 관계 연산 후 논리 연산 **[정답]**
   - (3) 모든 관계 연산 먼저
   - (4) `p == 1` 먼저 실행 후 관계 연산

**해설:** 논리 연산자(`&&`)는 관계 연산자보다 나중에 실행됩니다.

---

**문제 20:** `printf("%x", 255);`의 출력 결과는?
   - (1) 0xFF
   - (2) FF
   - (3) ff **[정답]**
   - (4) 255

**해설:** `%x`는 소문자로 16진수 출력하므로 `ff`가 출력됩니다.

---

**문제 21:** 변수 `int x = 7; x %= 3;` 후 `x`의 값은?
   - (1) 1
   - (2) 2 **[정답]**
   - (3) 3
   - (4) 4

**해설:** `x %= 3`은 `x`에 `7 % 3`의 나머지인 `2`를 저장합니다.

---

**문제 22:** 변수 `int x = 5, y = 3; int result = x / y;`의 `result` 값은?
   - (1) 1 **[정답]**
   - (2) 1.666...
   - (3) 2
   - (4) 0

**해설:** 두 정수의 나눗셈은 정수로 반환되므로 1이 됩니다.

---

**문제 23:** `x = (int)(5.6 / 2);`의 결과는?
   - (1) 2
   - (2) 2.8
   - (3) 3
   - (4) 2 **[정답]**

**해설:** `(int)` 형변환 후 정수 부분만 취해 2가 저장됩니다.

---

**문제 24:** `n <= 10 || k > 5 && p == 1`의 실행 순서는?
   - (1) `n <= 10`, `k > 5`, `p == 1`, 논리 연산 **[정답]**
   - (2) 논리 연산 후 관계 연산
   - (3) 관계 연산 후 `||`
   - (4) `&&` 먼저

**해설:** `&&`가 `||`보다 우선합니다.

---

**문제 

25:** `printf("%d", 'A');`의 결과는?
   - (1) A
   - (2) 65 **[정답]**
   - (3) 97
   - (4) ASCII

**해설:** 문자 `'A'`의 ASCII 코드 값은 65입니다.

---

추가해주신 내용을 바탕으로 각 개념을 설명하고 예제 문제 5개를 작성하겠습니다.

---

### 1. 선언과 초기화

- **선언**: 변수를 생성하고 자료형을 지정합니다. 예를 들어 `int num;`은 정수형 변수를 선언하는 코드입니다.
- **초기화**: 변수를 선언과 동시에 값을 대입하는 것입니다. 예를 들어 `int num = 10;`은 변수 선언과 동시에 값을 할당하여 초기화하는 코드입니다.

---

### 2. 형식 지정자와 필드 폭

- 형식 지정자 `%6.1f`에서 `6`은 출력할 자리 수(필드 폭), `.1`은 소수점 아래 표시할 자릿수를 의미합니다.
- 예를 들어 `printf("%6.1f", 456.456);`는 소수점 아래 한 자리까지 반올림하여 총 6칸에 출력하고, 결과는 `456.5`가 됩니다.

---

### 3. 상수를 표현하는 방법: 기호 상수와 매크로 상수

- **기호 상수**: `const` 키워드를 사용하여 상수를 선언합니다. 예: `const float PI = 3.14;`
- **매크로 상수**: `#define` 지시어를 사용하여 상수를 정의합니다. 예: `#define PI 3.14`
- 기호 상수는 값의 변경을 막는 반면, 매크로 상수는 전처리기에 의해 코드의 해당 부분이 상수로 치환됩니다.

---

### 4. 증감 연산자 (전치와 후치)

- 전치 연산(`++a`, `--a`): 변수를 사용하기 전에 값을 증가 또는 감소시킵니다.
- 후치 연산(`a++`, `a--`): 변수를 사용한 후 값을 증가 또는 감소시킵니다.

**예시**
```c
int a = 5;
int b = ++a; // b = 6, a = 6
int c = a++; // c = 6, a = 7
```

---

### 5. 실수 나누기와 정수 나누기

- **정수 나누기**: 두 정수를 나누면 결과는 정수형으로 출력되며 소수점 이하 값은 버려집니다.
- **실수 나누기**: 실수형 값을 사용하거나, 정수를 실수형으로 형 변환하면 소수점 이하까지 출력됩니다.

**예시**
```c
int a = 5, b = 2;
printf("%d\n", a / b);        // 출력: 2
printf("%f\n", (float)a / b); // 출력: 2.5
```

---

### 예제 문제와 해설

**문제 1**: 다음 중 변수를 선언하면서 동시에 값을 대입하는 것은?
   - (1) `int num;`
   - (2) `int num = 10;` **[정답]**
   - (3) `float PI;`
   - (4) `float = PI 3.14;`

**해설**: 선언과 초기화는 `int num = 10;`과 같이 변수 타입, 변수명, 초기값을 함께 지정하는 것을 의미합니다.

---

**문제 2**: `printf("%6.1f", 456.456);`의 출력 결과는?
   - (1)  456.5 **[정답]**
   - (2) 456.4
   - (3) 456.45
   - (4) 456.500

**해설**: `6.1f`에서 필드 폭은 6, 소수점 아래 자릿수는 1입니다. 따라서 `456.5`가 출력됩니다.

---

**문제 3**: 상수 정의 방식으로 올바르지 않은 것은?
   - (1) `#define PI 3.14`
   - (2) `const float PI = 3.14;`
   - (3) `float PI = 3.14;` **[정답]**
   - (4) `const int MAX = 100;`

**해설**: 상수는 `const` 키워드 또는 `#define` 지시어를 사용하여 정의합니다. `float PI = 3.14;`는 상수가 아닙니다.

---

**문제 4**: `int x = 5; int y = ++x;`에서 `y`와 `x`의 값은 각각 얼마인가요?
   - (1) `y = 5, x = 6`
   - (2) `y = 6, x = 5`
   - (3) `y = 5, x = 5`
   - (4) `y = 6, x = 6` **[정답]**

**해설**: `++x`는 전치 증가 연산으로 `x`를 먼저 증가시키고 `y`에 대입합니다. 따라서 둘 다 6입니다.

---

**문제 5**: `int a = 10; int b = 3; printf("%f", (float)a / b);`의 출력 결과는?
   - (1) 3.0
   - (2) 3.33
   - (3) 3.333333 **[정답]**
   - (4) 3

**해설**: `(float)a`로 실수형 나누기가 되어 소수점 이하까지 표시됩니다.








*if else문과 switch case문의 이해 if else 문을 switch case 문으로 변경 할 수 있는가?)

*전반적인 반복문의 활용의 이해
while, do-while, for문의 실행 결과 예측하기

*while 문과 for문에 대한 이해(while문과 for문을 서로 바꿀 수 있는가?)
->6주차 강의자료 p.121, p.122 확인




*while 다음에 나오는 조건식(보초값, 센티널)에 대한 이해.
->6주차 강의자료 p.91 – p.95 확인
해당 내용은 음수를 입력하면 반복문을 종료하겠지만, 
예를 들어 다른 값을 입력 했을 때 종료하게 변경?

*break와 continue 의 활용 법
break는 만나는 시점에 해당 반복문 탈출
continue는 만나는 시점에 해당 반복문의 종류에 따라 다름(p.153의 그림이 해당 내용)
 for문 : 증감식으로 이동하여 조건식 반복 여부 평가
 while문 : 조건식으로 이동하여 반복 여부 평가
 do while문 : do 블록은 반드시 실행하나 continue 만나면 바로 조건식 이동 후 반복 평가

//do-while문과 continue문 실행 예시
#include <stdio.h>
int main() {
    int i = 0;
    do {
        i++;
        if (i == 3) {
            continue;  // 조건식으로 이동
        }
        printf("i = %d\n", i);
    } while (i < 5);
    return 0;
}
 

*무한루프 만드는법
-while 문일 때
while(1){
    문장;
}

-for 문일 때 
for(;;){
    문장;
}

*/